## Transform Binary File to COE

This script converts an input binary file into a `.coe` file, a format commonly used by Vivado.

First create a virtual environment and install the required dependency:
```bash
python3 -m venv .venv
source ./.venv/bin/activate
pip install numpy
```

Then run the script:
```bash
python script.py
```
